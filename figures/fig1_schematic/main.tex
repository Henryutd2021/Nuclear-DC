% Fig 1 — 5-case system schematic (TikZ)
%
% Status: STUB. Design intent captured below; actual TikZ figure deferred.
% Compile target: standalone PDF that drops into the AE manuscript at 89mm
% single-column width.  Match sci-figure palette for case identity (so the
% schematic reads as the same family as Fig 2-8).
%
% ---- DESIGN INTENT ---------------------------------------------------------
% Five panels (one per case) arranged in a 2x3 grid (last cell blank or used
% for the unified legend / scale bar):
%
%   +---------+---------+---------+
%   |   C0    |   C1    |   C2    |
%   |  grid   |  BWRX   |  BWRX   |
%   |  -only  |  no HR  |  cogen  |   <- (hero panel, slight emphasis)
%   +---------+---------+---------+
%   |   C3    |   C4    | Legend  |
%   |  BWRX   |  NGCC   |  +scale |
%   |  +abs   | on-site |  bar    |
%   +---------+---------+---------+
%
% Each case panel shows:
%   - The energy source (BWRX-300 reactor / NGCC unit / ERCOT grid icon)
%   - The data center (rack icon at 200 MW)
%   - Energy flows: thick teal arrows = electricity, thick orange = heat
%   - Cooling subsystem: chiller / absorption / ORC where applicable
%   - Flow rates labelled on arrows (MW, MWth)
%
% Palette (must match notebooks/figure_helpers.py + sci-figure):
%   case0   = #B9B9B9   neutral baseline
%   case1   = #86DED4   intro nuclear (light teal)
%   case2   = #036868   hero (deep teal)
%   case3   = #224686   alt cogen (navy)
%   case4   = #A46A4D   NGCC (warm clay)
%   power   = stroke_teal  (#036868)   - electricity arrows
%   heat    = stroke_clay  (#A46A4D)   - thermal arrows
%   waste   = fill_gray    (#B9B9B9)   - dump / rejected heat
%
% ---- TODO LIST -------------------------------------------------------------
% [ ] Draw 5 case sub-panels in `tikzpicture` env with consistent
%     reactor / DC / cooling iconography
% [ ] Energy flow arrows (electricity teal, heat clay) with MW labels
% [ ] Unified legend cell (bottom-right)
% [ ] Scale bar (200 MW reference) if it helps reader calibrate
% [ ] No figure title (caption owns title text per Nature convention)
% [ ] Export: compile to PDF, drop into manuscript via \includegraphics
% ---------------------------------------------------------------------------

\documentclass[border=2mm]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning, shapes.geometric, arrows.meta, calc, fit}

% Palette (must match sci-figure NPPH2)
\definecolor{case0}{HTML}{B9B9B9}
\definecolor{case1}{HTML}{86DED4}
\definecolor{case2}{HTML}{036868}
\definecolor{case3}{HTML}{224686}
\definecolor{case4}{HTML}{A46A4D}
\definecolor{powerflow}{HTML}{036868}
\definecolor{heatflow}{HTML}{A46A4D}
\definecolor{wasteflow}{HTML}{B9B9B9}

\begin{document}
\begin{tikzpicture}[
    font=\sffamily\footnotesize,
    every node/.style={align=center},
    block/.style={rectangle, rounded corners=2pt, draw=black, line width=0.4pt,
                  minimum width=14mm, minimum height=8mm, inner sep=2pt},
    power/.style={-Stealth, line width=1.0pt, draw=powerflow},
    heat/.style ={-Stealth, line width=1.0pt, draw=heatflow},
    waste/.style={-Stealth, line width=1.0pt, draw=wasteflow, dashed},
]

% TODO: implement 5-case schematic
\node[block] (placeholder) at (0,0) {Fig 1 schematic\\ \scriptsize (stub --- see TODO at top of file)};

\end{tikzpicture}
\end{document}
